#include<stdio.h>
int main()
{
	int i,n,r,sum=0,num;
	scanf("%d",&num);
	n=num;
	while(n!=0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(num==sum)
	{
		printf("%d is a armstrongnumber",num);
	}
	else
	{
		printf("%d is not a armstrong number",num);
	}
	return 0;
}
